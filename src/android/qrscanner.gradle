repositories {
    jcenter()
}

dependencies {
    implementation 'com.journeyapps:zxing-android-embedded:3.3.0'
    implementation 'com.android.support:appcompat-v7:27.0.1'
}

android {
    buildToolsVersion '27.0.0' // Older versions may give compile errors
}